{
  "name": "bbin",
  "version": "1.0.33",
  "description": "bbin global installs",
  "license": "MIT OR Apache-2.0",
  "files": [
    "package.json"
  ],
  "scripts": {
    "preinstall": "npm show bbin dependencies --json && npm -g i $( npm show bbin dependencies --json | jq -r keys[])",
    "uninstall": "npm -g u $( npm show bbin dependencies --json | jq -r keys[])"
  },
  "dependencies": {
    "cloudflare-cli": "*",
    "semver": "*",
    "shaurl": "*",
    "spoof": "*",
    "vercel": "*",
    "wrangler": "*"
  }
}
